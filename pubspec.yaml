name: expandable_widgets
description: Expandable widget
version: 0.0.1
homepage: https://github.com/fatihozkan-github/ExpandableWidgets.git

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

assets:
  - assets/